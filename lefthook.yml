pre-commit:
  commands:
    fmt:
      run: cargo fmt --check
    clippy:
      run: cargo clippy --all-targets --all-features -- -D warnings

pre-push:
  commands:
    tests:
      run: cargo test
    build:
      run: cargo build --release
