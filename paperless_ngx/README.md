# paperless-ngx-cli package notes

The supported CLI entry point is `paperless`.

See the repository root [README](../README.md) for:

- installation and release notes
- the currently supported command surface
- Paperless endpoint mapping for implemented commands
- REPL/session behavior and JSON output expectations

This package intentionally exposes a focused subset of the Paperless-ngx API
rather than the entire backend surface.
